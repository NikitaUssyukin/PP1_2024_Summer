#include <iostream>

using namespace std;

int main() {

    int a, b = 5;

    cin >> a;

    if(a <= b) {
        cout << a << " ";
        ++a;
    }

    if(a <= b) {
        cout << a << " ";
        ++a;
    }

    if(a <= b) {
        cout << a << " ";
        ++a;
    }

    if(a <= b) {
        cout << a << " ";
        ++a;
    }

    if(a <= b) {
        cout << a << " ";
        ++a;
    }

    return 0;
}