#include <iostream>

using namespace std;

/*
getting multiple pieces of data 
as an input from the terminal
and
outputting multiple pieces of data to the terminal

to get input:
cin >> variable_a >> variable_b;

to output:
cout << variable_a << variable_b;
*/

int main() {
    int a;
    int b;

    cin >> a >> b;

    cout << a << ' ' << b;

    return 0;
}