#include <iostream>

using namespace std;

// conditional statements
int main() {
    
    int a, b;
    cin >> a >> b;

    if(a > b) {
        cout << a << " is more than " << b << "!\n";
    }
    else {
        cout << a << " is not more than " << b << "!\n";
    }
    

    return 0;
}

